fastapi==0.115.0
uvicorn[standard]==0.30.6
transformers==4.45.2
# torch is installed separately in docker/Dockerfile so the variant
# (cpu / cu121 / cu124 / etc) can be selected via build-arg without editing
# this file. For local non-Docker setups, install torch==2.5.1 yourself
# from the PyTorch index that matches your hardware before installing
# this requirements file.
accelerate==1.0.1
httpx==0.27.2
pydantic==2.9.2
numpy==1.26.4
eth-account>=0.11.3,<0.14
web3>=6.20,<8
pydantic-settings==2.5.2
