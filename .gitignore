/worker/__pycache__
/worker/tests/__pycache__
/orchestrator/__pycache__
.venv/

# Plans, specs, and PRDs — keep out of version control
docs/superpowers/
PRD-*.md
*.plan.md
*.spec.md
*.pyc
/.claude/worktrees

# Local secrets — never commit
.env
.env.local
*.env
!.env.example
keys.json
contracts/out/
contracts/cache/
contracts/broadcast/

# Mainnet artifacts — addresses + tx hashes go in MAINNET_DEPLOYMENT.md (tracked).
# .env.mainnet and the raw state file contain or could contain secrets/sensitive
# operator keys (when filled in) and stay local.
.env.mainnet
.mainnet_state.json
vendor/superfluid/
